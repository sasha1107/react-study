import {
    Accordion,
    AccordionItem,
    AccordionButton,
    Box,
    AccordionIcon,
    AccordionPanel,
    Card,
    CardBody,
    Divider
} from '@chakra-ui/react';
import SyntaxHighlighter from 'react-syntax-highlighter';
import atomOneDark from 'react-syntax-highlighter/dist/esm/styles/hljs/atom-one-dark.js';

export const ColorBox = ({children, color}) => (
    <div
        style={{
        backgroundColor: color,
        borderRadius: '8px',
        padding: '0.5rem 1.5rem',
        }}>
        {children}
    </div>
);

# Your First Component

## 컴포넌트란 ?

> UI를 만드는 블럭들

리액트는 마크업 + CSS + JS를 재사용 가능한 UI 요소인 component로 결합

-   내부적으로는 동일한 HTML 태그 사용
-   컴포넌트를 재사용하여 개발 속도를 높일 수 있다.

- - -

## Defining a component

### 전통적인 웹페이지 만들기

마크업 + JS로 인터랙션(상호작용) 추가

### React Component

-   리액트는 동일한 기술을 사용하면서, 인터랙션(상호작용)을 우선시
-   JSX 문법으로 작성(자바스크립트 내부에 마크업을 삽입)

> **리액트 컴포넌트**는 마크업을 뿌릴 수 있는 자바스크립트 함수이다.

> ⚠️ 리액트 컴포넌트는 일반 자바스크립트 함수지만, 함수 이름은 대문자로 시작해야 한다. 안그러면 작동하지 않음!

- - - 

## Nesting and organizing components


<SyntaxHighlighter language="javascript" style={atomOneDark}>
{`<Gallery>
    <Profile />
    <Profile />
    <Profile />
</Gallery>}`}
</SyntaxHighlighter>


`Gallery`가 부모 컴포넌트, `Profile`이 자식 컴포넌트

`Profile` 컴포넌트가 `Gallery` 컴포넌트 내부에서 렌더링된다.

> 🪄 리액트에서는 컴포넌트를 한 번만 정의하고, 여러 곳에서 여러 번 원하는 만큼 사용할 수 있다

<ColorBox color="#fef5e7">
### 🚨 주의

컴포넌트는 다른 컴포넌트를 렌더링할 수 있지만, 해당 정의를 중첩해서는 안 된다.

<SyntaxHighlighter language="javascript" style={atomOneDark}>
{`export default function Gallery() {
  // 🔴 Never define a component inside another component!
    function Profile() {
        // ...
    }
  // ...
}`}
</SyntaxHighlighter>

-   자식 컴포넌트가 부모로부터 데이터가 필요하다면, 중첩 정의대신 `props`로 전달한다.
</ColorBox>


<br/>
<Accordion allowToggle>
    <AccordionItem bg="rgb(243 244 253)" color="#575FB7">
        <AccordionButton _expanded={{ bg: '#575FB7', color: 'white' }}>
            <Box as='span' flex='1' textAlign='left'>
                **심화**
            </Box>
            <AccordionIcon />
        </AccordionButton>
        <AccordionPanel>
            리액트 애플리케이션은 `root` 컴포넌트에서 시작된다.
`root` 컴포넌트는 일반적으로 새로운 프로젝트를 시작할 때 자동적으로 생성된다.


- 예를 들어 CRA로 프로젝트를 생성하면 root 컴포넌트는 `src/App.js`이다.
- Next.js 프레임워크를 사용하는 경우 루트 구성요소는 `pages/index.js`애 정외된다. (13버전에서는 아마 `app/page.js`..)

대부분의 리액트 앱은 컴포넌트를 끝까지 사용한다.(all the way down)
> (small) 버튼 같은 재사용 가능한 부분에서부터 --> 전체 페이지까지 (large)

[리액트 기반의 프레임워크(Next.js, Remix, Gatsby 등)](https://react.dev/learn/start-a-new-react-project)는 빈 HTML 파일을 사용하고 JS로 페이지를 관리하도록 하는 대신, React 컴포넌트로부터 자동으로 HTML 파일을 생성한다.
-> 자바스크립트 코드가 로드되기 전에 앱에서 일부 콘텐츠를 표시할 수 있다.
        </AccordionPanel>
    </AccordionItem>
</Accordion>
<Divider/>

<Card>
    <CardBody>        
        ## 요약
        
        - 리액트를 사용하면 재사용 가능한 UI 요소인 컴포넌트를 만들 수 있다.
        - 리액트는 다음 규칙을 제외하면 일반적인 자바스크립트 함수이다.
            - 이름은 항상 대문자로 시작
            - JSX 마크업을 반환
    </CardBody>
</Card>

