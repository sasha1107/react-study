export const Box = ({children, color}) => (
    <div
        style={{
        backgroundColor: color,
        borderRadius: '8px',
        padding: '0.5rem 1.5rem',
        }}>
        {children}
    </div>
);